// Route configuration - ideally these would come from the backend /route endpoint
export const ROUTE_CONFIG = {
  name: 'Seattle to Boston via I-90',
  startCity: 'Seattle',
  endCity: 'Boston',
  // Center of continental US for initial map view
  mapCenter: [42.5, -96],
  mapZoom: 4,
};
